service : {
  addArticle: (text, text) -> ();
  clearArticles: () -> ();
  getArticle: (text) -> (opt text) query;
  getArticles: () -> (vec record {
                            text;
                            text;
                          }) query;
  searchArticles: (text) -> (vec record {
                                   text;
                                   text;
                                 }) query;
}
